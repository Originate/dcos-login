dependencies:
  override:
    - scripts/ci/prepare

  cache_directories:
    - "~/.cache"

test:
  override:
    - echo "No tests here :)"

deployment:
  master:
    branch: example
    commands:
      - echo "TODO"
